---
interface Props {
	date: Date;
}

const date = new Date(Astro.props.date);

---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-uk', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
